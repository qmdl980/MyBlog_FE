import "../../App.css"

const Pagination = ({limit, count, paginate}) => {

    return(
        <div className={"content-wrap"}>
            pagination
        </div>
    )
}
export default Pagination